# 海神服专用浏览器 · 多开逻辑分析（可复用参考）

> 用途：整理"海神服专用浏览器"的多开实现方式，作为**另一个项目多开设计**的参考（该项目窗口一多就闪退，本分析与同步器项目无关）。
> 分析对象：`海神服专用浏览器/批量启动.exe`、`海神服专用浏览器/海神服专用浏览器.exe`
> 方法：真实 PE 导入表静态分析（pefile）+ 资源字符串表（`.strings.txt`）。
> 置信度：高（结论基于真实二进制导入表）。运行时循环细节需动态调试才能 100% 还原，见末尾"未确认项"。

---

## 一、一句话结论

**多开 = `批量启动.exe`（主控）用 `CreateProcess` 拉起 N 个独立的 `海神服专用浏览器.exe`（工作进程），每个进程只负责一个游戏窗口，主控再用 `MoveWindow`/`SetWindowPos` 把窗口平铺排版。**

浏览器本体**不会自己复制自己**：它没有 `CreateProcess` 导入，只"在自己的进程里建一个窗口"。

→ **关键设计：每个窗口 = 一个独立进程（进程级隔离）。这是它窗口开再多也不闪退的根本原因。**

---

## 二、架构：主控 + 工作进程（角色分离）

| 角色 | 程序 | 职责 |
|------|------|------|
| **主控 / 多开器** | `批量启动.exe` | 决定开几个、拉起进程、排版窗口、切焦点、关闭时回收 |
| **工作进程 / 单窗口** | `海神服专用浏览器.exe` | 只跑"自己这一个窗口"，独立渲染游戏，不关心还有几个兄弟 |

> 复用要点：**把一个"多开管理"逻辑和一个"窗口渲染"逻辑拆成两个程序**。主控崩溃也不会连累窗口；单个窗口崩溃也只丢一个，不影响其他。

---

## 三、主控（批量启动.exe）干了什么

导入的关键能力（来自真实 PE 导入表）：

| 能力 | 导入函数 | 作用 |
|------|----------|------|
| **拉起进程** | `CreateProcessA`、`WinExec`、`ShellExecuteA` | 启动多个浏览器工作进程 |
| **枚举/管理** | `FindWindowExA`、`GetWindowThreadProcessId`、`OpenProcess`、`TerminateProcess` | 找到窗口 HWND、按需关闭/回收 |
| **窗口排版** | `MoveWindow`、`SetWindowPos`、`SetParent`、`GetWindowRect` | 把多开窗口摆到不同坐标/显示器 |
| **焦点切换** | `SetForegroundWindow`、`ShowWindow` | 切到指定窗口 |
| **多显示器** | `GetMonitorInfoA`、`EnumDisplayMonitors`、`MonitorFromWindow` | 跨屏平铺 |
| **热键** | 字符串表含 `Ctrl+Shift+F1~F12` 等一串 | 按热键聚焦第 N 个窗口 |

→ 典型的 MFC（VC++）多开管理器：自身不渲染任何内容，只做"生孩子 + 排座位 + 叫号 + 收尸"。

---

## 四、工作进程（浏览器本体）干了什么

| 能力 | 导入函数 | 作用 |
|------|----------|------|
| **动态加载内核** | `LoadLibraryA`、`GetProcAddress` | 运行时加载 `libacf2.dll`（Chromium 封装），解析 `acfCreateBrowser` 等 |
| **窗口管理** | `MoveWindow`、`ShowWindow`… | 只管理"自己这一个窗口" |
| **无 `CreateProcess`** | —— | 不会启动其他进程 |

`libacf2.dll` = Chromium Embedded Framework 封装层，API 族：`acfInitialize`、`acfCreateBrowser`/`acfCreateBrowserSync`、`acfGetBrowserCount`、`acfCloseAllBrowsers`、`acfRunMessageLoop` 等。

→ **每个工作进程 = 一个独立的 Chromium 渲染实例**，加载同一个 H5 游戏（地址硬编码在资源里）。

---

## 五、完整多开流程（推断组装）

```
[批量启动.exe]  读取配置（开几个、坐标/显示器）
   │
   ├─ CreateProcess ──► [浏览器.exe #1] ──► libacf2 ──► Chromium 渲染窗口①
   ├─ CreateProcess ──► [浏览器.exe #2] ──► libacf2 ──► Chromium 渲染窗口②
   ├─ ...（重复 N 次）
   │
   ├─ FindWindowEx + GetWindowThreadProcessId ── 拿到每个窗口 HWND
   ├─ MoveWindow / SetWindowPos                ── 平铺到不同位置/显示器
   ├─ Ctrl+Shift+F1..F12 热键                  ── 聚焦第 N 个窗口
   └─ TerminateProcess（关闭时）               ── 逐个回收进程
```

每个窗口是**独立会话**（各自 cookie/缓存），互不干扰、互不同步输入。

---

## 六、为什么它窗口开再多也不闪退（核心，给你另一个项目看）

根本原因只有一条：**进程级隔离 + 每窗口一个独立进程**，而不是某种巧妙优化。

1. **每窗口独立进程（最关键）**
   一个窗口的页面卡死 / 内存涨 / 崩溃，**不会**拖垮其他窗口——它们不共享地址空间、不共享主线程、不共享资源句柄。单个进程挂了，OS 直接回收，其余照常。

2. **渲染/绘制独立**
   每个工作进程自带独立的 GPU/渲染子进程，主线程不被绘制阻塞，一个卡了不影响别的。

3. **主控无渲染负担**
   多开管理器自己不画任何东西，只做轻量的进程拉起 + 窗口排版，N 再大也只是多几次 `CreateProcess`/`MoveWindow`，几乎零成本。

4. **无跨窗口共享状态**
   窗口之间没有共享内存、共享句柄、共享输入管线。共享越少，一处出错越不会连锁。

> **对照你那个会闪退的项目**：如果它是"一个进程里 `CreateWindow` 出 N 个窗口"，那窗口一多就崩几乎是必然的——句柄/GDI 对象/内存都挤在同一个进程地址空间里，一个窗口出错（泄漏、异常、资源耗尽）就会把整个进程拖死。海神服的解法就是：**别挤一个进程，拆成 N 个独立进程。**

---

## 七、可直接复用到你项目的设计清单

若要把海神服这套多开方式借鉴到"窗口一多就闪退"的项目，建议：

1. **把"多开管理"和"窗口渲染"拆成两个程序**
   - 主控：只负责拉进程、排版、回收（参考 `批量启动.exe`）。
   - 工作进程：每个只负责一个窗口（参考 `海神服专用浏览器.exe`）。

2. **用 `CreateProcess` 拉起 N 个独立工作进程**，而不是在一个进程里 `CreateWindow` N 次。
   - 这是解决"窗口多了闪退"最根本的一步：故障域隔离，一个挂不影响整体。

3. **主控用 `FindWindowEx` + `GetWindowThreadProcessId` 枚举/持有窗口 HWND**，用 `MoveWindow`/`SetWindowPos` 排版，`TerminateProcess` 关闭时回收。

4. **跨显示器平铺**用 `EnumDisplayMonitors` + `GetMonitorInfo`，避免所有窗口挤在主屏。

5. **每个工作进程独立会话/资源**（独立配置、独立缓存目录），避免互相踩。

6. **若必须单进程多窗口（改不动架构）**，至少做到：
   - 严格释放 GDI 对象 / 句柄（常见闪退真凶：`HBITMAP`/`HDC`/`HFONT` 泄漏导致 GDI 耗尽）；
   - 每个窗口的消息处理相互独立、不阻塞主消息循环；
   - 单个窗口出异常时局部 `try/catch` 隔离，不让异常冒泡杀掉整个进程。

---

## 八、未确认项（需动态调试才能 100% 还原）

1. 主控具体开几个窗口、坐标/数量从哪读（配置文件/注册表/界面输入）——需运行或查资源对话框。
2. 每个工作进程如何分配不同会话（缓存目录为空，可能运行时写入或走登录态）。
3. 浏览器虽暴露 `acfGetBrowserCount`/`acfGetBrowserObjectAt`（支持"单进程多浏览器视图"），但实际多开走的是"多进程"方案——未验证是否也用了单进程多视图。
4. 热键到窗口的精确映射（F1..F12 对应第几个 HWND）。

> 以上需对 `批量启动.exe` 做动态分析（运行 + API Monitor / 调试器）或反编译 `.text` 段才能确认，本次仅做静态导入 + 资源分析。

---

## 附：分析产物与方法

- `analyze_pe.py`（`_pe_imports.py` 简化版）：用 `pefile` 提取两个 exe 的导入表。
- `海神服_提取/*.strings.txt`：从原 exe 提取的资源/字符串表（含 Chromium flags、H5 URL、热键表、ACF API 名）。
- 置信度：导入表与字符串表是二进制里**确定存在**的内容，结论可靠；运行时控制流为推断。
