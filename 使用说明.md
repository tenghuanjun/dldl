1、使用说明
IP池https://dash.cloudflare.com/
确认 Worker 已部署：访问 https://red-moon-ff95.tenghuanjun.workers.dev/get-ip 应返回 {"ip":"..."}
如未部署，使用 cloudflare-worker.js 重新部署：
npm install -g @cloudflare/wrangler
wrangler login
wrangler deploy

备用IP池
2、https://api.ipify.org/?format=json&time=4129596000000

3、数据库使用的是supabase

---

## UINFO / HISTORY Cookie 审查结论（2026-06-08）

### 背景
快捷登录功能通过 Electron 主进程在打开 37.com.cn 窗口前注入 UINFO/HISTORY cookie，使页面识别为"已登录"状态。需确认这些 cookie 的注入不会触发远程登录 API，从而避免消耗 IP 登录限制配额。

### Cookie 读写行为（bundle.js）

| 操作 | 代码位置 | 何时触发 | 是否发 API 请求 |
|------|---------|---------|:---:|
| **读取 UINFO** (`getUserInfo()`) | 行 14154-14165 | 仅在用户交互打开登录 UI 时 | ❌ 只读 cookie |
| **读取 HISTORY** (`getHistoryAccount()`) | 行 14147-14153 | 仅在 insertHistoryAccount 被调用时 | ❌ 只读 cookie |
| **写入 UINFO** (`saveUserInfo()`) | 行 14120-14124 | 仅在登录成功回调中 | ❌ 只写 cookie |
| **写入 HISTORY** (`insertHistoryAccount()`) | 行 14127-14145 | 仅在登录成功回调中 | ❌ 只写 cookie |
| **UINFO/HISTORY 用于 setcookie** | 行 14030-14046 | 登录成功 + gid 属于 [1005367, 1005515, 1005290, 1005281] 时 | ✅ POST h5sdk/setcookie |

### 页面加载时实际发起的请求

| 请求 | 是否自动触发 | 说明 |
|------|:---:|------|
| `Sdk.init()` 激活接口 | ✅ 是 | JSONP 获取游戏/API 配置，非登录接口 |
| SDKReport 埋点 (`active` 事件) | ✅ 是 | 加载 `//37.com.cn/sdk-report/dist/bundle.js` |
| `h5sdk/login` | ❌ 否 | 仅用户主动提交用户名密码时触发 |
| `h5sdk/query_login` | ❌ 否 | 定义了但完全未被调用 |
| `autoassign` | ❌ 否 | 仅用户主动触发时调用 |
| `h5sdk/setcookie` | ❌ 否 | 仅特定 gid 登录成功后触发 |

### 快捷登录各操作 API 触发情况

| 操作 | 已有 UINFO | 已有 token | 是否调 h5sdk/login |
|------|:---:|:---:|:---:|
| 快捷登录（注入 cookie） | ✅ | N/A | ❌ 不会 |
| 打开扫码页 | 有 | 无 | ✅ 会（fetchToken 触发） |
| 打开扫码页 | 有 | 有 | ❌ 不会（用缓存） |
| 刷新 token | 有 | - | ❌ 走 /api/token/refresh 本地代理 |
| 复制网址 | 有 | 无 | ✅ 会（fetchToken 触发） |

### 结论（初版，已被修正）

~~**UINFO 和 HISTORY 是纯 cookie 存储，设置/读取不会触发任何远程 API。** 快捷登录注入 cookie 是安全的，不会消耗 IP 登录限制配额。消耗配额的是 `h5sdk/login`，仅在用户主动提交登录表单时触发。~~

---

## ⚠️ 重要修正：快捷登录确实会触发 h5sdk/login（2026-06-08）

### 问题根因
快捷登录打开 `https://37.com.cn/h5game/public/?pid=46&gid=1005176`（**没有 wv 参数**），触发以下调用链：

```
bundle.js Partner.init(pid=46)
  → 第 13548-13551 行: pid=46 无 wv 参数 → pid 强制改为 1
  → 第 13585-13605 行: pid=1 走 else 分支 → 动态加载 chunk 0.js
  → require("./1") 加载 pid=1 联运商 partner
  → partner.init() 检测到 UINFO cookie 已存在 → 自动触发登录
  → 调用 h5sdk/login ❌ 消耗 IP 登录配额！
```

### 关键代码路径（bundle.js）

| 行号 | 代码 | 作用 |
|------|------|------|
| 13548-13551 | `pid=46 && !wv → pid=1` | 无 wv 参数时，pid=46 被降级为 pid=1 |
| 13557-13569 | `pid=46 && wv=2` | wv=2 时走 native 路径（不加载 pid=1 partner） |
| 13585-13605 | else 分支 | pid=1 动态加载联运商 partner |
| 13836-13841 | `User.api.login = 'h5sdk/login/'` | pid=1 partner.init() 会调用此接口 |

### 解决方案（最终 v5，2026-06-08）
**参考 proxy.js 的 `/pc/getCodeInfo` 拦截模式：将 JSONP 请求 redirect 到本地 Express 代理，由代理返回伪造响应。**

`wv=2` 方案不可行（会走 iOS native Bridge，Electron 环境白屏）。
`cancel: true` 方案不可行（JSONP 请求被取消后页面一直转圈等待回调）。
**v3 `data:` URL redirect 方案不可行**：Chromium 禁止 `<script src="data:...">`。
**v4 `protocol.handle` 自定义协议方案不可行**：独立 partition session 的 protocol handler 可能未正确生效。

**最终方案 v5**：在 Electron 主进程用 `webRequest.onBeforeRequest` 将 `h5sdk/login` 和 `query_login` 的 JSONP 请求 redirect 到本地 Express 代理（`http://127.0.0.1:PORT/api/h5sdk/login/fake`），由 Express 返回标准的 JSONP 格式伪造响应。这是最可靠的方式——走标准 HTTP 请求-响应流程，不受 Chromium 安全策略限制。

```javascript
// === electron-main.js ===
const localProxyBase = `http://127.0.0.1:${PORT}`;

quickSession.webRequest.onBeforeRequest(
  { urls: ['*://s-api.37.com.cn/h5sdk/login*'] },
  (details, callback) => {
    const urlObj = new URL(details.url);
    const redirectUrl = localProxyBase + '/api/h5sdk/login/fake' + urlObj.search;
    callback({ redirectURL: redirectUrl });
  }
);

quickSession.webRequest.onBeforeRequest(
  { urls: ['*://s-api.37.com.cn/h5sdk/query_login*'] },
  (details, callback) => {
    const urlObj = new URL(details.url);
    const redirectUrl = localProxyBase + '/api/h5sdk/query_login/fake' + urlObj.search;
    callback({ redirectURL: redirectUrl });
  }
);

// === proxy.js ===
app.get('/api/h5sdk/login/fake', (req, res) => {
  const callbackName = String(req.query.callback || 'jsonp_callback_0');
  const fakeResp = { state: 1, msg: '成功', data: { token: 'FAKE_...', ... } };
  const respBody = callbackName + '(' + JSON.stringify(fakeResp) + ');';
  res.setHeader('Content-Type', 'application/javascript; charset=utf-8');
  res.end(respBody);
});
```

**注意事项**：
- 伪造的 `token`/`sign` 是占位值，不影响真实游戏登录（游戏登录通过 `openLogin` → `dldl.50pk.com/login.php` 使用 Supabase 中存储的真实 token）
- 快捷登录的目的仅是让 37 门户页面完成初始化、识别 UINFO cookie，不触发 IP 限制

### 相关文件
- `electron-main.js` — cookie 注入逻辑（第 64-170 行 openQuickLoginWindow）
- `bundle.js` — SDK 初始化与 API 调用逻辑（第 13548-13569 行 Partner.init pid 判断）
- `account.html` — 快捷登录 / 打开扫码页前端逻辑（第 1205 行 portalUrl）