# 定时自动刷新说明文档

## 概述

定时刷新功能自动扫描数据库中 URL 即将过期的账号，通过 h5sdk 通道重新获取游戏登录链接，确保 token 永不过期。

## 架构

```
Cron Triggers (每6小时)
    │
    ▼
主 Worker (red-moon-ff95)
    │
    ├── 查询 Supabase → 所有有 URL 的账号
    ├── 筛选过期账号（url_created_at > 65小时）
    ├── 逐个调用 doLoginCore() 刷新
    └── 写回 Supabase 更新 url / token / sign
```

## 触发方式

| 方式 | 说明 |
|------|------|
| **自动定时** | 每 6 小时自动执行（`0 */6 * * *`） |
| **手动触发** | 浏览器访问 `https://red-moon-ff95.tenghuanjun.workers.dev/api/cron-refresh` |

## 刷新逻辑

```
1. 查询 Supabase：select accounts where url is not null，按 url_created_at 升序
2. 遍历每个账号：
   ├── 跳过未过期（url创建 < 65小时）→ 不浪费 IP 配额
   ├── 跳过缺密码（upwd 为空）→ 计为错误
   └── 执行刷新：
        ├── SDK 登录（尝试获取 uid，失败不影响）
        ├── h5sdk 登录 → 获取新 token
        ├── PC 通道写入参数
        └── 写回 Supabase（url、token、sign、url_created_at）
3. 每次刷新间隔 5 秒（避免触发 37 服务器 IP 限制）
```

## 配置参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 定时频率 | `0 */6 * * *` | 每 6 小时执行 |
| 过期阈值 | 65 小时 | URL 创建超过 65h 视为过期 |
| Token 有效期 | 72 小时 | 提前 7 小时刷新，留缓冲 |
| 刷新间隔 | 5 秒 | 两次刷新之间等待 |
| 查询范围 | 全量 | 所有有 URL 的账号 |

## 关键代码位置

**文件**: `cloudflare-worker.js`

| 函数 | 行号位置 | 作用 |
|------|---------|------|
| `refreshExpiredAccounts()` | ~780 | 定时刷新主函数 |
| `doLoginCore()` | ~700 | 登录核心（Cron 和 HTTP 共用） |
| `sdkLogin()` | ~550 | SDK 登录 |
| `h5sdkLogin()` | ~590 | h5sdk 登录 |
| `pcPostCodeInfo()` | ~620 | PC 通道写入 |
| `pcGetCodeInfo()` | ~640 | PC 通道读取 |

**Cron 配置** (`wrangler.toml`):
```toml
[triggers]
crons = ["0 */6 * * *"]
```

## 数据库依赖

- **表**: `accounts`
- **必要字段**: `id`, `uname`, `upwd`, `url`, `url_created_at`
- **RLS**: 已关闭（`DISABLE ROW LEVEL SECURITY`），Worker 可读全部字段
- **API Key**: anon key

## 返回数据结构

手动触发 `/api/cron-refresh` 返回：

```json
{
  "scanned": 31,        // 扫描到的有URL账号数
  "refreshed": 3,       // 成功刷新数
  "errors": 0,          // 失败数
  "detail": [
    { "totalWithUrl": 31 },
    { "expiredCount": 3, "missingCreds": 0, "hasBothCount": 31 },
    // 如有失败，每项含 { "uname": "xxx", "error": "原因" }
  ]
}
```

## 安全性

- Cron 仅在主 Worker (`red-moon-ff95`) 运行，其他 99 个轮询 Worker 不执行定时任务
- `/api/cron-refresh` 无鉴权，但不暴露敏感信息
- 密码 (`upwd`) 仅在 Worker 内存中使用，不在响应中返回

## 多 Worker 协同

```
定时刷新（仅主Worker）
    │
    ▼
写回 Supabase（所有Worker共用数据库）
    │
    ▼
前端读取（自动看到新 token）
```

主 Worker 刷新后写入 Supabase，前端加载时自动读取到最新的 URL 和 token。
