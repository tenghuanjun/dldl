# 28手游斗罗 APK 反编译分析文档

> 对比对象：斗罗大陆官网版（37 系，`反编译分析文档.md`）
> 核心结论：28 手游是**独立平台**，账号体系在 `mobile.28zhe.com`，登录接口、鉴权方式、密码处理与 37 系**完全不同**——这就是"用我们软件的登录接口登 28 账号报账号密码错误"的根因。

---

## 一、反编译概述

### 1.1 APK 基本信息
- **文件名**：`28手游斗罗.apk`（41.95 MB）
- **包名**：`com.cy.yyjia.zhe28`
- **DEX 数量**：4 个（`classes.dex` 13MB / `classes2.dex` 8.3MB / `classes3.dex` 8.1MB / `classes4.dex` 4.6MB）
- **三方 SDK 痕迹**：快手/极速版广告、腾讯 QQ/微信登录、巨量引擎(火山)、阿里云号码认证(中国移动 sim 卡一键登录)、微信支付等

### 1.2 反编译工具链
- **jadx 1.5.5**（Chocolatey 安装，支持 `--single-class` 精准反编译）
- **PowerShell .NET `ZipFile`**（解压 APK，绕开 `Expand-Archive` 拒绝 `.apk` 扩展名的限制）
- **Python 3.12**（DEX 字符串扫描辅助）

### 1.3 反编译代码保留位置
| 目录 | 内容 |
|---|---|
| `dldl28_apk_extract/` | APK 原始资源完整解压（2227 个文件，含 `assets/`、`res/`、`AndroidManifest.xml`、4 个 DEX） |
| `dldl28_src_c3/` | `classes3.dex` 反编译 Java 源码（**含全部登录/账号逻辑**） |
| `dldl28_src/` | 完整 4 个 DEX 反编译 Java 源码（统一目录，便于全量检索） |
| `dldl28_apk_extract/strings_scan.txt` | 全 DEX 关键词字符串扫描（含 `28zhe`、`login`、`api` 等） |
| `dldl28_apk_extract/focused_scan.txt` | 聚焦业务（排除 androidx/kotlin/okhttp 框架）的登录/加密字符串 |
| `tools/scan28.py` / `tools/scan28_focused.py` | 解压 + DEX 字符串扫描辅助脚本 |

---

## 二、反编译流程

```bash
# 1. 解压 APK 原始资源（PowerShell 直接读 zip，规避 .apk 扩展名限制 + 自动覆盖重复条目）
powershell -NoProfile -Command "Add-Type -AssemblyName System.IO.Compression.FileSystem; `
  [System.IO.Compression.ZipFile]::ExtractToDirectory('28手游斗罗.apk','dldl28_apk_extract')"

# 2. 反编译 DEX（只反编译代码、不解码资源以提速）
jadx -d dldl28_src --no-res -j 4 "28手游斗罗.apk"

# 如需单独反编译某个 dex（更快、便于精准分析）：
jadx -d dldl28_src_c3 --no-res "dldl28_apk_extract/classes3.dex"

# 快速定位登录相关字符串：
python tools/scan28_focused.py
```

### 关键定位路径（本次分析实际走的路径）
1. 扫描 DEX 字符串 → 命中 `https://mobile.28zhe.com/api/v1/`、`https://mobile.28zhe.com/api/v2/`、`user/login`
2. 反编译含 `user/login` 的 `classes3.dex` → 得到 `com.cy.yyjia.zhe28.util` 包
3. 读 `Repository.login()` → 参数 `username/password/version`
4. 读 `NetUtil.java` → 确认 `BASE_URL1` 常量、请求方式、鉴权头、响应解析

---

## 三、核心常量

| 常量 | 值 | 来源 |
|---|---|---|
| `NetUtil.BASE_URL1` | `https://mobile.28zhe.com/api/v1/` | `NetUtil.java:62` |
| `NetUtil.BASE_URL2` | `https://mobile.28zhe.com/api/v2/` | `NetUtil.java:63` |
| `NetUtil.BASE_URL3` | `https://www.28zhe.com/` | `NetUtil.java:64`（网页/H5 用） |
| 包名 | `com.cy.yyjia.zhe28` | AndroidManifest |
| 登录结果类 | `LoginResult` | `com.cy.yyjia.zhe28.domain.LoginResult` |
| Token 来源 | `Constant.INSTANCE.getToken()` | `com.cy.yyjia.zhe28.util.Constant` |

---

## 四、登录接口（重点）

### 4.1 接口地址
```
POST https://mobile.28zhe.com/api/v1/user/login
```
由 `NetUtil.BASE_URL1 + "user/login"` 拼接（`Repository$login$$inlined$post$1.java:95`）。

### 4.2 请求构造

**参数组装**（`Repository.java:152-157`）：
```java
LinkedHashMap linkedHashMap = new LinkedHashMap();
linkedHashMap.put("username", username);
linkedHashMap.put("password", password);
linkedHashMap.put("version", "2");
NetUtil netUtil = NetUtil.INSTANCE;
// 调用 post("user/login", linkedHashMap) → 内部 BASE_URL1 + "user/login"
```

**实际网络请求**（`Repository$login$$inlined$post$1.java:102-103`）：
```java
String token = Constant.INSTANCE.getToken();
Response responseExecute = ((PostRequest) ((PostRequest) OkGo.post(str)
    .headers("Authorization", "bearer " + token))   // ← 统一鉴权头
    .params(map, new boolean[0])).execute();        // ← 表单参数，明文，无加密
```

### 4.3 请求要素汇总
| 项 | 值 |
|---|---|
| 方法 | `POST` |
| URL | `https://mobile.28zhe.com/api/v1/user/login` |
| Content-Type | `application/x-www-form-urlencoded`（OkGo `params`） |
| 请求头 | `Authorization: bearer <token>`（token 来自 `Constant.getToken()`，登录态前可能为空串 → `bearer `） |
| 参数 `username` | 账号 |
| 参数 `password` | 密码（**明文，无 AES/无签名加密层**） |
| 参数 `version` | 固定 `"2"` |

> **注意**：`NetUtil` 的所有请求（login/get/post）都不会对参数做签名或加密，仅统一附加 `Authorization: bearer` 头。第三方 AES（`com/tencent`、`com/mobile/auth`、`com/nirvana/tools`）均为微信/移动认证/游戏引擎库，与账号密码登录无关。

### 4.4 响应解析
统一结构（`NetUtil$request$type$1` → `BaseResult<T>`）：
```json
{ "code": 200, "msg": "成功", "data": { /* LoginResult */ } }
```
- `code == 200` → 成功，`data` 为 `LoginResult`（含 token、uid 等）
- `code == 403` → 未登录/过期 → 跳转 `LoginActivity`
- `code == 4004` → 打开 `data` 字段中的网页 URL（`toUrl`）
- 其他 → 业务提示（`toOther`，如"绑定手机""实名认证"）

### 4.5 其他登录/注册相关接口（同 `Repository.java`）
| 接口（BASE_URL1 + 路径） | 方法 | 关键参数 |
|---|---|---|
| `/user/codeLogin` | POST | `telephone_number`, `telephone_code`, `reg_type=phone`, `i_mei`, `channelUid`, `bdvid`, `game_id`, `invite_uid`, `formType`, `android_id`, `version=2` |
| `/wxLogin` | POST | `appid`, `secret`, `code`, `grant_type=authorization_code` |
| `/getWxAccessToken` | GET | `appid`, `secret`, `grant_type=refresh_token`, `refresh_token` |
| `/wxBindOrRegister`（微信绑定） | POST | `openid`, `nick_name`, `reg_type=weixin`, `os_type`, `i_mei`, `channelUid`, `game_id`, `avatar`, `union_id`... |
| `/phoneBindOrRegister`（手机绑定） | POST | `access_token`, `reg_type=phone`, `os_type`, `i_mei`, `channelUid`, `game_id`... |

> `codeLogin`/`wxLogin` 等同样返回 `BaseResult`，且都通过 `NetUtil` 带 `bearer` 头，逻辑一致。

---

## 五、与 37 系（斗罗大陆官网）的根本差异（报错根因）

| 维度 | 37 系（斗罗大陆.apk） | 28 系（28手游斗罗.apk） |
|---|---|---|
| 账号服务器 | `s-api.37.com.cn` | `mobile.28zhe.com` |
| 登录接口 | `/sdk/login/`（SignV3 拼接） | `/api/v1/user/login` |
| 鉴权方式 | 无 bearer，靠签名校验 | `Authorization: bearer <token>` |
| 密码处理 | AES/ECB/PKCS5Padding + Base64（APP_KEY 前 16 位） | **明文** |
| 请求签名 | SignV3（参数按 key 排序 + 追加 APP_KEY + MD5） | **无签名** |
| 公共参数 | pid/gid/refer/dev/scut 等 | version/channelUid/game_id 等（多为业务字段） |
| 响应字段 | `state` / `msg` / `data` | `code` / `msg` / `data` |
| 加密库 | 自研 `EncryptUtil`/`AESUtil` | 仅三方库（微信/移动认证/游戏引擎），账号登录无加密 |

**结论**：两套账号体系、服务器、接口、鉴权、加密完全独立。用 37 的 `/sdk/login/` 去验证 28 平台的账号，服务端根本查不到该账号 → 返回"账号或密码错误"。**要让 28 账号在我们软件里登录，必须改用 28 的 `https://mobile.28zhe.com/api/v1/user/login` + bearer 鉴权 + 明文密码，而非 37 的 SignV3/AES 流程。**

---

## 六、关于"扫码进入游戏"流程

用户反馈"后续扫码那个流程是一样的"——指登录成功后的 **token → 扫码进入游戏** 环节与现有 proxy 的 PCLauncher 扫码流程机制一致（APP 扫码 PC 二维码、服务端关联会话、PC 轮询拿到游戏入口参数）。

- 本次已定位 **账号密码登录接口**（第四章），这是 28 与 37 唯一不同的环节。
- 登录成功返回 `LoginResult`（含 token/uid），其后的扫码/游戏入口逻辑建议对接时再针对性反编译 `LoginActivity` / `tokenLogin` 进一步核实，但预期与现有 proxy 兼容。

---

## 七、文件清单

| 文件/目录 | 说明 |
|---|---|
| `28手游斗罗反编译分析文档.md` | 本文档 |
| `dldl28_apk_extract/` | APK 资源完整解压（保留原始代码/资源） |
| `dldl28_src_c3/` | `classes3.dex` 反编译 Java 源码（含登录逻辑，核心分析对象） |
| `dldl28_src/` | 完整 4 DEX 反编译 Java 源码 |
| `dldl28_apk_extract/strings_scan.txt` | DEX 全量关键词字符串 |
| `dldl28_apk_extract/focused_scan.txt` | 聚焦业务登录/加密字符串 |
| `tools/scan28.py`、`tools/scan28_focused.py` | 解压 + DEX 字符串扫描脚本 |
