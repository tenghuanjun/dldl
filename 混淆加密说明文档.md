# 加密混淆说明文档（桌面端 DLDL-Proxy）

> 用途：记录程序的「混淆加密」机制与构建流程，便于出问题时**定位源头、快速解决**。
> 维护人见此文档即知：源码在哪、产物在哪、运行时加载的是哪一份、打包命令是什么、常见故障怎么排查。

---

## 一、整体思路

程序由 **Electron** 打包成 Windows 安装包（`DLDL-Proxy Setup x.x.x.exe`）。代码在发布前经过一层 **JS 混淆加密**，核心目标是：

1. **隐藏敏感逻辑**：主界面 `account.html` 内联脚本里含有 supabase key、登录鉴权、账号排序等核心逻辑，发布出去不能是明文可读。
2. **增加逆向成本**：所有字符串（key、接口路径、字段名）经 **RC4 加密**后存入字符串数组，运行时才解密；局部变量/函数名改为**十六进制**（`_0x1a2b` 之类），人类不可读。
3. **不破坏功能**：刻意**不重命名全局变量、不重命名对象属性**，避免 Vue 模板绑定、DB 字段名、Electron `contextBridge` 暴露的接口名断裂。

> 术语说明：这里的“加密”是 **javascript-obfuscator 的混淆（含 RC4 字符串加密）**，不是运行时强加密。它的作用是**提高阅读/篡改门槛**，不是防住专业逆向。asar 包本身只是只读归档，解开即见混淆后代码。

---

## 二、文件清单（源码 vs 产物）

### 源码（可读、可编辑，应纳入版本控制）
| 文件 | 作用 |
|------|------|
| `account.html` | 主界面，含一个内联 `<script>`（主应用逻辑，含 supabase key） |
| `preload.js` | 主窗口桥接脚本（`contextBridge` 暴露原生能力给页面） |
| `quick-login-preload.js` | 快捷登录窗口桥接脚本 |
| `electron-main.js` | Electron 主进程（窗口管理、APP 登录子进程、preload 选择） |
| `proxy.js` | 本地 Express 服务，静态资源 + 代理；决定 serve 哪个 html |
| `tools/obfuscate-desktop.mjs` | **桌面端混淆脚本**（本文重点） |
| `tools/obfuscate.mjs` | 移动端混淆脚本（`mobile.html` → `mobile.min.html`） |

### 混淆产物（构建生成，**建议加 `.gitignore`，不要手改**）
| 文件 | 来源 | 说明 |
|------|------|------|
| `account.min.html` | `account.html` | 内联主脚本被替换为 `<script src="account.min.js">` |
| `account.min.js` | `account.html` 内联脚本 | 主逻辑混淆（RC4 + hex 重命名） |
| `preload.min.js` | `preload.js` | 桥接脚本混淆 |
| `quick-login-preload.min.js` | `quick-login-preload.js` | 桥接脚本混淆 |

> ⚠️ 产物是**自动生成**的。改功能请改 `account.html` / `*.js` 源码，再重跑混淆脚本，**不要直接改 `*.min.*`**。

---

## 三、混淆脚本逻辑（`tools/obfuscate-desktop.mjs`）

脚本做两件独立的事：

### 1) 主界面：`account.html` → `account.min.html` + `account.min.js`
- 用正则 `<script(?![^>]*\bsrc=)[^>]*>...</script>` 匹配**无 `src` 属性的内联脚本**。
- 只处理含 `createApp({` 的那个块（即 Vue 主应用），其余内联脚本（如外部库）不动。
- 把该块代码交给 `obfuscate()`，产物写 `account.min.js`。
- 把原 html 里那块内联脚本替换为 `<script src="account.min.js"></script>`，写 `account.min.html`。

### 2) 桥接脚本：`preload.js` / `quick-login-preload.js` → `.min.js`
- 逐个读、混淆、写出同名 `.min.js`。

### 关键安全措施（OBF_OPTS，`obfuscate-desktop.mjs` 第 15–28 行）
```js
compact: true,
stringArray: true,
stringArrayEncoding: ['rc4'],   // 字符串 RC4 加密，运行时解密
stringArrayThreshold: 1,        // 所有字符串都进加密数组
identifierNamesGenerator: 'hexadecimal', // 局部变量/函数名 → _0x.. 十六进制
renameGlobals: false,           // 不重命名全局（Vue/CryptoJS/ElementPlus/Sortable/supabase 等）
renameProperties: false,        // 不重命名属性名（Vue 模板绑定/DB 字段/contextBridge 名不断）
controlFlowFlattening: false,   // 关，避免性能与兼容风险
deadCodeInjection: false,       // 关，避免体积膨胀
selfDefending: false,           // 关，避免被二次压缩/格式化后失效
debugProtection: false,         // 关，避免调试时被卡死
disableConsoleOutput: false     // 不屏蔽 console，便于排错
```

### 防呆保护
脚本第 33 行校验：若混淆产物长度 `< 源码长度 * 0.2`，判定异常直接抛错终止，**防止把源码“混淆没了”导致打包后崩溃**。

---

## 四、运行时加载的是哪一份

打包/运行时的加载优先级，决定了“看到的是源码还是混淆版”：

### 主界面（`proxy.js` 第 1232 行）
```js
app.get('/account.html', (req, res, next) => {
  const minHtml = path.join(staticRoot, 'account.min.html');
  if (fs.existsSync(minHtml)) return res.sendFile(minHtml); // 有混淆版优先
  next(); // 否则回退 account.html 源码
});
```
→ **存在 `account.min.html` 就 serve 它，否则回退 `account.html`。**

### 桥接脚本（`electron-main.js` 的 `pickPreload`）
```js
function pickPreload(name) {
  const min = path.join(__dirname, name.replace(/\.js$/, '.min.js'));
  return fs.existsSync(min) ? min : path.join(__dirname, name); // 有 .min 优先
}
```
→ 两个 `preload` 引用都走 `pickPreload`，**有 `.min.js` 就加载混淆版。**

> 这意味着：开发态（仓库里没有 `.min` 文件）自动用源码，**方便调试**；发布态（构建后生成 `.min`）用混淆版。

---

## 五、构建打包流程（产出 exe）

一条命令完成「混淆 + 打包」：
```powershell
npm run pack
```
等价于 `package.json` 中：
```json
"pack": "node tools/obfuscate-desktop.mjs && electron-builder --win --x64"
```
顺序：
1. `obfuscate-desktop.mjs` 生成 4 个 `.min.*` 产物。
2. `electron-builder --win --x64` 用 NSIS 模板打包，产物在 `dist-electron/`：
   - `DLDL-Proxy Setup 1.0.0.exe`（单个安装包，双击即装）
   - `win-unpacked/`（免安装绿色版，含 `DLDL-Proxy.exe` + `resources/app.asar`）
3. 混淆产物被 `package.json` 的 `files` 数组（第 46–49 行）纳入 asar 包。

> 移动端独立命令：`npm run build` → `tools/obfuscate.mjs`（只出 `mobile.min.html`，不打包 exe）。

---

## 六、排错指南（出问题先来这）

### 问题 1：功能正常但怀疑“没混淆 / 还是明文”
- **确认**：安装目录 `resources/app.asar` 解开后是否有 `account.min.js` / `account.min.html`。
- 快速验证：`npm run pack` 后跑
  ```powershell
  node node_modules/@electron/asar/bin/asar.js list "dist-electron/win-unpacked/resources/app.asar" | Select-String "min\.(html|js)$"
  ```
  应列出 4 个 min 文件。
- 若没有 → 检查 `package.json` 的 `files` 是否含这 4 个文件；或 `obfuscate-desktop.mjs` 是否报错中断。

### 问题 2：打包后运行崩溃 / 白屏
- **先验证混淆产物语法**：`node --check account.min.js` / `preload.min.js` / `quick-login-preload.min.js`，必须全部 `OK`。
- 若 `node --check` 报错 → 多半是混淆选项太激进。默认已设保守（`renameGlobals/Properties:false`）。如自行加 `controlFlowFlattening:true` 等激进项，先关回去重测。
- 看 `build.log` 里打包是否真的 EXIT=0。
- 用**未混淆版**临时验证：删掉 `account.min.html` 和三个 `.min.js` 再打包 → 自动回退源码，若此时正常则说明问题在混淆步骤（再细查哪个文件）。

### 问题 3：改了 `account.html` 源码，打包后功能没变
- 原因：`account.min.*` 是旧产物，运行时优先加载它。
- 解决：重新跑 `npm run pack`（脚本会重新生成 min 产物），**不要只跑 `electron-builder`**。

### 问题 4：排序 / 数据不生效（与混淆无关，但常混淆）
- 账号拖拽顺序靠数据库 `accounts.sort_order` 列持久化：
  - `loadAccounts` 按 `.order('sort_order')` 读；
  - `saveAccounts` 写 `sort_order: ai`（区服内下标）；
  - 保存后 `onAccountCardsSorted` 会**立刻从 DB 重读**暴露写库失败。
- 若保存后排序回退 → 多半是 **Supabase 加列后没刷新 schema 缓存**，新列被 `update` 静默丢弃。
  - 验证：`SELECT id, area_name, uname, sort_order FROM public.accounts LIMIT 20;` 看 `sort_order` 是否变化。
  - 解决：SQL Editor 跑 `NOTIFY pgrst, 'reload schema';` 或后台点 “Reload schema cache”。

### 问题 5：构建失败 `nsis-resources` 下载超时（GitHub 连不上）
- 现象：`win-unpacked/` 已生成，但“下载 nsis 资源”那步网络超时，EXIT=1。
- 解决：**重跑一次**即可（nsis 本体已缓存，通常第二次能下到资源）。命令：
  ```powershell
  .\node_modules\.bin\electron-builder.cmd --win --x64
  ```
- 若持续超时：挂代理或手动下载 `nsis-resources-*.7z` 放进 electron-builder 缓存目录。

### 问题 6：安装/运行时被 SmartScreen 拦截
- 原因：**未做代码签名**（无证书）。非病毒，点“仍要运行”即可。
- 彻底消除需配置 EV 代码签名证书并在 `electron-builder` 的 `win.certificateFile` 指定。

### 问题 7：想本地直接运行看效果（不安装）
- 用 `dist-electron/win-unpacked/DLDL-Proxy.exe`（绿色版），免安装。

---

## 七、常见问题速查表

| 现象 | 优先检查位置 | 一句话对策 |
|------|------|------|
| 还是明文代码 | `npm run pack` 是否执行；`files` 是否含 min | 重跑 pack 并确认 asar 内 4 个 min 文件 |
| 运行崩溃/白屏 | `node --check *.min.js` | 关掉激进混淆项重测 |
| 改源码无效 | 旧的 `account.min.*` | 重跑 `npm run pack` |
| 排序回退 | Supabase `sort_order` 列值 | 跑 `NOTIFY pgrst,'reload schema'` |
| 打包失败 | `nsis-resources` 下载 | 重跑 electron-builder |
| SmartScreen 警告 | 代码签名 | 配证书或点“仍要运行” |

---

## 八、后续可优化（可选）
1. **产物进 `.gitignore`**：`account.min.html`、`account.min.js`、`preload.min.js`、`quick-login-preload.min.js` 为生成物，可忽略避免误提交。
2. **代码签名**：消除 SmartScreen 警告。
3. **asar 加密**：electron-builder 支持 `asar` + 自定义 unpack，可进一步增加解包成本（仍非强加密）。
4. **supabase key 进一步保护**：若 key 泄露风险高，可改为运行时经后端代理下发，而非写死在前端混淆产物里。
