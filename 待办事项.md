# 待办事项 - 部署手机端刷新APPtoken功能

## 背景

手机端（`mobile.html`）部署到 GitHub Pages 后，点击「刷新APPtoken」会请求 `/api/app-login`，但 GitHub Pages 是纯静态托管，没有后端，导致返回 405。

桌面端不受影响（通过本地 proxy.js 代理处理）。

## 已完成的工作

1. ✅ `cloudflare-worker.js` — 已重写，新增 `/api/app-login` 接口（SDK 登录 + PC 扫码流程）
2. ✅ `mobile.html` — 已将请求地址从 `/api/app-login` 改为 `WORKER + '/api/app-login'`，`WORKER` 指向 `https://red-moon-ff95.tenghuanjun.workers.dev`

## 待处理

### 第一步：部署 Cloudflare Worker

在终端运行：

```bash
cd /Users/tiffany/dldl
npx wrangler deploy
```

运行后会自动打开浏览器跳转到 Cloudflare 授权页面，点击授权即可。  
如果浏览器打不开，可以用 API Token 方式：

1. 打开 https://dash.cloudflare.com/ 登录
2. 右上角头像 → My Profile → API Tokens
3. 点 Create Token → 使用「Edit Cloudflare Workers」模板
4. 复制生成的 token，运行：
```bash
npx wrangler deploy --token <你的API_TOKEN>
```

### 第二步：推送前端代码到 GitHub Pages

```bash
cd /Users/tiffany/dldl
git add mobile.html cloudflare-worker.js
git commit -m "手机端刷新APPtoken走Cloudflare Worker"
git push github tauri
```

（如果你的 GitHub Pages 部署方式和 commit 命令不同，按你平时的流程操作即可）

### 第三步：验证

在手机上打开 GitHub Pages 部署的 mobile 页面，登录后点击「刷新APPtoken」，确认不再报 405 错误。

## 涉及的文件

| 文件 | 作用 | 是否已改 |
|------|------|----------|
| `cloudflare-worker.js` | Worker 后端，处理 /api/app-login | ✅ |
| `mobile.html` | 手机端页面，请求指向 Worker | ✅ |
| `proxy.js` | 桌面端代理，不受影响 | 未改 |
| `index.html` | 桌面端页面，不受影响 | 未改 |
