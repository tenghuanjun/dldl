# 待办事项 - 部署手机端新功能

## 背景

手机端（`mobile.html`）部署到 GitHub Pages 后，缺少后端支持，需要将以下功能通过 Cloudflare Worker 实现：

1. **刷新APPtoken**：`/api/app-login` — SDK 登录 + PC 扫码流程，生成游戏入口 URL
2. **扫描通行证码**：`/api/pass-code` — 手机扫码读取 PC 端通行证二维码，完成通行证验证

桌面端不受影响（通过本地 proxy.js 代理处理）。

## 已完成的工作

1. ✅ `cloudflare-worker.js` — 已重写，新增 `/api/app-login` 和 `/api/pass-code` 接口
2. ✅ `mobile.html` — 请求指向 Worker；新增「扫码」按钮 + 摄像扫描功能

## 待处理

### 第一步：部署 Cloudflare Worker

在终端运行：

```bash
cd /Users/tiffany/dldl
npx wrangler deploy
```

运行后会自动打开浏览器跳转到 Cloudflare 授权页面，点击授权即可。  
如果浏览器打不开，可以用 API Token 方式：

1. 打开 https://dash.cloudflare.com/ 登录
2. 右上角头像 → My Profile → API Tokens
3. 点 Create Token → 使用「Edit Cloudflare Workers」模板
4. 复制生成的 token，运行：
```bash
npx wrangler deploy --token <你的API_TOKEN>
```

### 第二步：推送前端代码到 GitHub Pages

```bash
cd /Users/tiffany/dldl
git add mobile.html cloudflare-worker.js
git commit -m "手机端支持刷新APPtoken和扫码通行证"
git push github tauri
```

（如果你的 GitHub Pages 部署方式和 commit 命令不同，按你平时的流程操作即可）

### 第三步：验证

- **刷新APPtoken**：手机上登录 → 点击「刷新APPtoken」→ 确认不再报 405
- **扫码通行证**：PC端打开通行证页面生成二维码 → 手机上点击「扫码」→ 扫描二维码 → 确认显示"通行证验证成功"

## 涉及的文件

| 文件 | 作用 | 是否已改 |
|------|------|----------|
| `cloudflare-worker.js` | Worker 后端，处理 /api/app-login + /api/pass-code | ✅ |
| `mobile.html` | 手机端页面，请求指向 Worker，新增扫码功能 | ✅ |
| `proxy.js` | 桌面端代理，不受影响 | 未改 |
| `index.html` | 桌面端页面，不受影响 | 未改 |

## 后续优化建议

### 定时刷新接口鉴权

当前 `/api/cron-refresh`（cloudflare-worker.js:964）在 Worker 端**无鉴权**，任何人直接 `curl https://red-moon-ff95.tenghuanjun.workers.dev/api/cron-refresh` 即可触发。前端已通过 `users.is_admin` 字段控制「定时刷新」按钮的显示（不再明文判断 `username === 'tenghuanjun'`），但接口本身仍对公网裸奔。

**建议方案（二选一）：**

1. **加 header 校验（轻量）**：Worker 端校验请求头 `x-cron-secret`，前端 `handleCronRefresh` 调用时带上。注意 secret 写在 `mobile.html` 里只是混淆，不是真安全。改动 worker 后需重新 `wrangler deploy` 才能生效。
   ```js
   if (url.pathname === '/api/cron-refresh') {
     if (request.headers.get('x-cron-secret') !== CRON_SECRET) {
       return jsonResponse({ error: 'unauthorized' }, 401);
     }
     const result = await refreshExpiredAccounts(env);
     return jsonResponse(result);
   }
   ```
2. **改为 Worker 定时触发器（cron trigger）**：由 Cloudflare 定时自动跑，根本不对公网开放，安全性最高。

**状态：暂不处理**（维持现状，接口仍公开，仅前端按钮受 `is_admin` 控制）。

## 前端脚本混淆（推荐）

为防止前端逻辑/密钥被轻易阅读，已加入混淆构建流程：

- 构建脚本：`tools/obfuscate.mjs`（使用 `javascript-obfuscator`）
- 用法：`npm run build`（等价于 `node tools/obfuscate.mjs`）
- 产物：
  - `mobile.min.html` —— 混淆版页面（主逻辑改为引用外部脚本）
  - `app.min.js` —— 抽出的主逻辑混淆脚本（RC4 加密字符串、标识符重命名）

混淆配置要点：`renameGlobals:false` / `renameProperties:false`（保留 Vue 全局与 DB 字段名绑定）；字符串全部 RC4 加密，源码中不再有 `tenghuanjun`、`supabase` 密钥等明文。

### 部署注意事项

- **生产环境应部署 `mobile.min.html` + `app.min.js` 两个文件**（二者需放同一目录，页面用相对路径 `app.min.js` 引用），而不是未混淆的 `mobile.html`。
- `mobile.html` 仅为可编辑源码，日常功能修改在它上面进行，发布前跑一次构建脚本生成混淆产物。
- 依赖 `javascript-obfuscator` 是用 `--no-save` 安装的（未写入 package.json）。若重装了 `node_modules`，需重新 `npm i javascript-obfuscator --no-save` 才能运行构建脚本。

### 第二步（更新）：推送混淆产物到 GitHub Pages

```bash
cd /Users/tiffany/dldl
npm run build                     # 先生成 mobile.min.html + app.min.js
git add mobile.min.html app.min.js
git commit -m "发布混淆版前端"
git push github tauri
```

（如果你的 GitHub Pages 部署方式和 commit 命令不同，按你平时的流程操作即可，但务必同时带上 `app.min.js`。）

---

## B 方案：套餐过期/禁用的服务端强制校验（防绕过）

**状态：暂不做**（当前采用 A 方案：前端 5 分钟定时器 + 本地缓存对比，见下方"已完成"）。此条记录 B 方案的完整实施方案，将来需要时直接照做。

### 背景 / 要解决的问题

担心用户在套餐过期后"卡 bug 一直用"。A 方案的校验全在前端（`account.html`），懂技术的用户改 HTML / DevTools / 断网制造查询失败（fail-open）即可绕过。B 方案把校验下沉到 **Electron 主进程 + 云端 service_role 查询**，显著提高绕过门槛。

### 已完成（A 方案，作为对照）

- ✅ 登录时拒绝过期套餐（`account.html` `handleLogin`）
- ✅ 每 **5 分钟**定时器静默校验 `checkAccountStatus()`，禁用/过期自动退出（`account.html` mounted，间隔 `300000`）
- ✅ `checkAccountStatus()` 成功后把状态写入 `localStorage['dldl_status_check']`
- ✅ 新增 `assertActive()`：优先读本地缓存对比，仅无 5 分钟内新鲜记录才联网复核；**管理员（isAdmin）全程无阻断**
- ✅ 顶部「刷新列表」`refreshAccounts` 与卡片「刷新APPtoken」`refreshToken` 改用 `assertActive()`
- ⚠️ 局限：联网复核失败时仍 fail-open（放行）；纯前端可被 patch 绕过

### B 方案要做的三件事

#### 1. 数据库（几乎不用动）

`users` 表已有 `enabled` / `plan_type` / `plan_expire_at`，判定禁用/过期已够，**必改项 = 0**。

可选增强（非必须）：
- `entitlement_version`（int，默认 0）：管理员改套餐/禁用时 +1；客户端缓存"上次放行版本号"，版本不符强制复核，防止长期不复核。
- `last_active_check`（timestamp）：审计用。
- 如需审计日志可新建 `entitlement_logs` 表（否则不用建表）。

#### 2. 服务器（核心工作量）

部署一个鉴权端点（**Supabase Edge Function** 最省事，已有云端项目、免域名/免服务器；备选 Cloudflare Worker / 现有 Node 服务）：

- 函数 `verify-entitlement`：入参 `{ userId }` + 调用者 anon JWT → 用 **service_role key** 查 `users`（绕过 RLS、查询条件前端不可篡改）→ 判禁用/过期 → 返回 `{ entitled: bool, reason }`。
- 函数内需验证"调用者身份 == 被查用户"，防止越权查他人。
- 配置 CORS 允许桌面客户端调用。
- 部署：项目里目前**没有** `supabase/config.toml`，需在 Supabase 控制台直接写函数，或用 CLI `supabase link` + `supabase functions deploy verify-entitlement`。

> ⚠️ **安全红线**：`service_role` key **只能放服务端函数环境变量，绝不能进 Electron 打包产物（asar）**。否则用户能从 asar 抠出 key 直接改数据库，等于没防。客户端调用只带用户 anon JWT。
> （注：`electron-main.js` 当前完全没连 Supabase，搜索 `createClient`/`SUPABASE_URL` 均 0 结果，需新引入。）

#### 3. 客户端（`electron-main.js`，Python 不用改）

在两个"真正动手"的 IPC 入口，spawn/启动前先调用云端校验，过期则拒绝：

- **`open-app-login` IPC**（`electron-main.js` 约 372 行，`openAppLoginWindow` 在此 spawn 游戏子进程）：过期 → 拒绝 spawn 并返回错误。
- **`sync-start` IPC**（约 562 行，开始窗口同步）：过期 → 拒绝启动。

为什么只卡这俩就够：游戏登录请求最终发往斗罗官方 `s-api-secure.37.com.cn`，无法在官方请求里插校验；唯一可控拦截点就是"主进程 spawn 窗口 / 发起同步前"。`sync_engine.py` 的同步严格由 `sync-start` 触发，入口卡住后 **Python 侧无需改动**。

### 防绕过强度评估（诚实）

- 当前（纯前端 A 方案）：改 HTML/DevTools 即可绕过 —— 门槛最低。
- B 方案后：校验在主进程 + 云端 service_role 查询，门槛升到"逆向 patch exe 或伪造网络响应" —— 显著提升。
- **非绝对**：精通者仍可 patch 主进程或抓包伪造校验响应。绝对安全需官方配合，或把游戏登录代理到自有服务器（先校验再转发官方，但改架构且官方接口未必允许代理）。

### 工作量估算

- 数据库：~0（可选加 1-2 列）
- 服务器函数：~50-100 行 + 部署约 10 分钟
- 客户端：2 个 IPC handler 各加 ~10 行，Python 0 改动
- 合计：约小半天

